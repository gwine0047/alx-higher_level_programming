--script will list all the records in the table: second_table
--display in this order, the score and the name
--ordered by the score
-- database name will be passed as an argument of the command

SELECT score, name FROM second_table ORDER BY score DESC;